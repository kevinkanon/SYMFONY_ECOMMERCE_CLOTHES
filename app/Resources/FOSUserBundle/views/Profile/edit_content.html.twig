{# surcharge de la vue de la route /profile/edit de FOSUser afin de modifier les informations du profil 
(email - mot de passse - nom utilisateur #} 
{% trans_default_domain 'FOSUserBundle' %}

<div class="row">

    <div class="span3">
        {% include 'UtilisateurBundle:Default:modulesUsed/utilisateurConnecte.html.twig' %}
    </div>

    <div class="span9">
        <h2>Mes informations</h2>
        <div id="collapseOne" class="accordion-body collapse in">
            <div class="accordion-inner">

                <div class="span4">
                    {{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }} {{ form_widget(form)}}
                    <div>
                        <input class="btn btn-primary" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

{#{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }} {{ form_widget(form)
}}
<div>
    <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
</div>
{{ form_end(form) }}#}